// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Set the site property to your full GitHub Pages URL
  site: 'https://michalfapso.github.io', // Replace with your actual username if different
  // Set the base path to your repository name
  base: '/vlk_uradne_tabule/', // Make sure this matches your repository name!
});
